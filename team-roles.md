# 两人协作分工方案

## 1. 目的

本文档用于在现有《伤寒论》知识图谱系统基础上，明确两名开发者的职责边界、交付物、协作方式与阶段安排。

分工依据来自两部分：

- 当前项目主架构：[ARCHITECTURE.md](D:/code/python/challenge/ARCHITECTURE.md)
- 新规划方向：[项目规划.docx](D:/OneDrive/Desktop/项目规划.docx)

当前原则不是简单按“前端 / 后端”切分，而是按“主链路职责”分工，保证项目主线持续推进：

```text
数据清洗 -> 标注复核 -> 模型抽取 -> 图谱构建 -> 系统展示 -> 迭代再训练
```

---

## 2. 当前系统基线

目前仓库已经具备以下可运行模块：

- Django 后端：`corpus`、`graph`、`modeling`、`annotation`
- Vue 前端页面：
  - `/` 总览页
  - `/explore` 图谱浏览与模型抽取页
  - `/models` 模型工作台
  - `/annotations` 候选复核页
- 数据与模型脚本：
  - 预标注、银标准清洗、图谱导出、Neo4j 导入
  - NER / RE 数据准备、训练、预测
- 已跑通主闭环：
  - 文本输入
  - NER 抽取
  - 图谱候选映射
  - RE 辅助判断
  - 会话图导出
  - 提交候选记录
  - 复核 accepted
  - accepted 数据导出

因此当前阶段的任务重点，不是重新从零设计系统，而是：

1. 稳定数据与模型链路
2. 完善系统交互与复核闭环
3. 形成可持续迭代的数据回流机制

---

## 3. 分工原则

### 3.1 不按纯前后端切

这个项目的核心难点不是普通 CRUD，而是“数据 + 模型 + 图谱 + 系统”的闭环。如果一个人只做前端、另一个人只做后端，容易出现：

- 前端页面有了，但核心数据质量跟不上
- 模型能跑，但系统无法沉淀有效复核数据
- 图谱导入了，但页面与业务流程脱节

因此更合理的方式是按主链路分工。

### 3.2 两条主线

建议拆为两条并行主线：

- **A 线：数据 / 模型 / 图谱主线**
- **B 线：系统 / 交互 / 复核闭环主线**

---

## 4. 角色分工

## 4.1 甲：数据、模型、图谱负责人

### 核心目标

保证项目“可训练、可建图、可迭代”。

### 负责范围

- 文本清洗与规则维护
- 银标准语料清洗与修正
- accepted 数据导出与训练集构建
- NER / RE 数据准备、训练、评估、误差分析
- 规则抽取与模型后处理
- Neo4j 导入、图谱结构维护、Cypher 查询
- 图谱与模型相关后端接口

### 主要目录归属

- `backend/scripts/`
- `backend/modeling/`
- `backend/graph/`
- `backend/corpus/`
- `data/`
- `models/`
- `experiments/`

### 当前重点任务

1. 将 accepted 候选记录转成 NER / RE 增量训练集
2. 优化 RE 数据质量，优先减噪，不优先堆训练轮数
3. 维护图谱导入和图谱查询接口
4. 输出稳定的模型版本和实验结论

### 交付物

- 训练集 / 验证集 / 测试集版本
- 模型 checkpoint 与结果摘要
- 图谱节点边导出文件
- Cypher 查询脚本
- 误差分析文档

---

## 4.2 乙：系统、前端、复核闭环负责人

### 核心目标

保证项目“可用、可演示、可沉淀数据”。

### 负责范围

- Vue 页面与交互流程设计
- 图谱浏览页、模型工作台、候选复核页
- 会话图导出、候选记录提交、筛选与状态流转
- 前后端 API 联调
- 系统操作路径、使用说明、文档整理
- 后续若扩展问答或智能检索入口，优先由乙负责页面与交互层

### 主要目录归属

- `frontend/`
- `backend/annotation/`
- `backend/core/`
- `docs/`
- `README.md`

### 当前重点任务

1. 优化 `/explore` 页的抽取、映射、批量关系判断体验
2. 优化 `/annotations` 页的复核效率与列表筛选
3. 增加 accepted 数据查看、导出入口和状态提示
4. 维护系统说明、使用说明、开发协作文档

### 交付物

- 可运行前端页面
- API 调用与状态提示完整的前端交互
- 候选复核工作流页面
- 使用说明与协作文档

---

## 5. 目录级责任划分

| 模块 | 主要负责人 | 说明 |
| ---- | ---------- | ---- |
| `backend/scripts` | 甲 | 数据脚本、训练脚本、图谱导入导出 |
| `backend/modeling` | 甲 | NER / RE 服务与模型接口 |
| `backend/graph` | 甲 | 图谱查询、图谱统计、Neo4j 对接 |
| `backend/corpus` | 甲 | 语料读取、搜索、预处理接口 |
| `backend/annotation` | 乙主、甲配合 | 候选记录流转、accepted 回流 |
| `frontend/src/views/GraphExplorerView.vue` | 乙主、甲配合接口 | 抽取、映射、RE、会话图 |
| `frontend/src/views/ModelWorkbenchView.vue` | 乙主、甲提供模型状态 | 模型状态展示与推理调用 |
| `frontend/src/views/AnnotationReviewView.vue` | 乙主 | 复核工作台 |
| `frontend/src/views/HomeView.vue` | 乙主 | 总览、案例展示 |
| `docs/` | 乙主、甲提供技术内容 | 使用说明、协作、实验总结 |
| `data/` `models/` `experiments/` | 甲 | 数据、模型、实验资产 |

---

## 6. 接口边界

为了避免反复改接口，双方需要遵守以下边界。

### 6.1 甲负责定义的数据结构

以下内容由甲主导定义，乙按此消费：

- 实体类型与关系类型
- 模型预测返回格式
- 图谱节点与关系字段
- accepted 导出数据格式

### 6.2 乙负责定义的页面需求

以下内容由乙主导提出，甲配合补接口：

- 页面展示字段需求
- 列表筛选条件
- 状态流转交互
- 会话图展示所需的聚合字段

### 6.3 必须先同步的变更

以下变更禁止单边直接修改后上线：

- 实体类型增删
- 关系类型增删
- 模型接口字段改名
- Neo4j 节点 / 边字段改名
- accepted 导出结构变更

---

## 7. 开发节奏建议

## 7.1 并行推进节奏

### 阶段一：稳定主闭环

- 甲：accepted -> 训练集转换、RE 数据减噪
- 乙：复核页与抽取页可用性优化

### 阶段二：提升系统可迭代性

- 甲：重训 NER、整理可用 RE 辅助策略
- 乙：补 accepted 记录查看、导出、统计入口

### 阶段三：扩展展示能力

- 甲：更稳定的图谱查询与案例结果
- 乙：系统总览、图谱展示、模型结果展示统一化

---

## 7.2 每周交接点

建议每周固定同步以下内容：

1. 本周新增了哪些数据或模型版本
2. 本周接口字段是否发生变化
3. 前端页面新增了哪些依赖字段
4. accepted 数据量与可重训样本量变化
5. 下周主目标是否仍围绕主闭环推进

---

## 8. Git 协作建议

当前仓库已采用：

- `main`：稳定版本
- `dev`：日常集成版本

建议分支命名：

- 甲：
  - `codex/feature-data-*`
  - `codex/feature-model-*`
  - `codex/feature-graph-*`
- 乙：
  - `codex/feature-frontend-*`
  - `codex/feature-annotation-*`
  - `codex/feature-demo-*`

### 提交流程

1. 从 `dev` 拉分支
2. 在自己的职责范围内开发
3. 提交 PR 到 `dev`
4. 通过后合并
5. 阶段稳定后再由 `dev` 合到 `main`

### 禁止事项

- 不直接改 `main`
- 不在未同步的情况下修改公共数据结构
- 不随意提交模型大文件、临时数据和本地配置

---

## 9. 当前最推荐的任务拆分

如果从现在立刻开始两人并行，建议这样安排：

### 甲本周任务

1. 新增 accepted -> NER 训练集转换脚本
2. 新增 accepted -> RE 训练集转换脚本
3. 重新整理 RE 数据质量，输出一版更干净的关系训练输入
4. 固定模型目录与实验结果记录格式

### 乙本周任务

1. 完善 `/annotations` 复核效率
2. 完善 `/explore` 的批量关系判断结果展示
3. 增加 accepted 记录导出后的前端查看能力
4. 补系统使用说明与协作说明

---

## 10. 结论

结合新规划文件与当前已落地系统，最合理的两人分工不是：

- 一个人只写前端
- 一个人只写后端

而是：

- **甲负责“数据-模型-图谱”**
- **乙负责“系统-交互-复核闭环”**

这样的分工更符合当前项目实际状态，也更能保证项目继续沿着这条主线推进：

```text
数据回流 -> 模型优化 -> 图谱更新 -> 系统展示 -> 再沉淀数据
```

这也是当前阶段最重要的工程目标。
